# linux.yml 在 ubuntu runner 上验证 Linux 构建 + 测试（含 race）+ arm64 交叉编译 + tarball 打包。
# 公开仓库 GitHub Actions 免费。autostart_linux.go 仅 Linux 编译，本工作流守护其正确性。
name: Linux 构建

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 设置 Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: go build + vet + test（含 race）
        run: |
          go build ./...
          go vet ./...
          go test -race ./...

      - name: arm64 交叉编译验证
        run: GOOS=linux GOARCH=arm64 go build -o /dev/null ./cmd/autosync

      - name: 打包 tarball（amd64 + arm64，对齐 Makefile package-linux）
        run: |
          for arch in amd64 arm64; do
            mkdir -p dist/autosync-linux-${arch}
            GOOS=linux GOARCH=${arch} CGO_ENABLED=0 go build -o dist/autosync-linux-${arch}/autosync ./cmd/autosync
            cp autosync.conf.example.yaml scripts/install-linux.sh scripts/README-install-linux.md dist/autosync-linux-${arch}/
            tar -czf dist/autosync-linux-${arch}.tar.gz -C dist autosync-linux-${arch}
          done

      - name: 上传 tarball
        uses: actions/upload-artifact@v4
        with:
          name: autosync-linux
          path: dist/autosync-linux-*.tar.gz
