# 构建产物
*.exe
*.test
*.out
/dist/
*.dmg

# macOS Xcode 工程（xcodegen 生成，不入库）
/macos/AutoSync.xcodeproj/

# 日志与状态文件
autosync.log
autosync.state.json

# 本地配置（可能含敏感信息；保留 config.example.yaml 作模板）
config.yaml

# Go
/vendor/

# byproduct 位于用户数据目录；此处忽略仓库根意外生成的本地 byproduct 残留
/autosync.conf.yaml
/autosync.state-*.json
/autosync.lock-*
/autosync.daemon.lock

# 编辑器/工具备份
*~
